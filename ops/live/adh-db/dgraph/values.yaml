fullnameOverride: dgraph

zero:
  replicaCount: 3
  persistence:
    size: 3Gi

alpha:
  replicaCount: 3
  service:
    type: NodePort
  persistence:
    size: 10Gi
  extraFlags: "--security whitelist=0.0.0.0/0"
